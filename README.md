# mnemos
Mnemos is a vector database written in Rust designed for efficient similiarity search on high-dimensional embeddings.

The initial version focus on a single-node vector search engine, with later goal of extending the system into a distributed vector database.